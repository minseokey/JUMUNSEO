import 'package:flutter/material.dart';

class CommunityDetailComment extends StatelessWidget {
  const CommunityDetailComment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
